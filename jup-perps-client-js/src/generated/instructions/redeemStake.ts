/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit'
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core'
import { PERPETUALS_PROGRAM_ADDRESS } from '../programs'

export const REDEEM_STAKE_DISCRIMINATOR = new Uint8Array([
  178, 203, 250, 105, 133, 118, 255, 69,
])

export function getRedeemStakeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REDEEM_STAKE_DISCRIMINATOR)
}

export type RedeemStakeInstruction<
  TProgram extends string = typeof PERPETUALS_PROGRAM_ADDRESS,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TAccountPerpetuals extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountCustody extends string | AccountMeta<string> = string,
  TAccountStakeAccount extends string | AccountMeta<string> = string,
  TAccountStakeInfo extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountKeeper extends string
        ? ReadonlySignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      TAccountPerpetuals extends string
        ? ReadonlyAccount<TAccountPerpetuals>
        : TAccountPerpetuals,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountCustody extends string
        ? WritableAccount<TAccountCustody>
        : TAccountCustody,
      TAccountStakeAccount extends string
        ? ReadonlyAccount<TAccountStakeAccount>
        : TAccountStakeAccount,
      TAccountStakeInfo extends string
        ? WritableAccount<TAccountStakeInfo>
        : TAccountStakeInfo,
      ...TRemainingAccounts,
    ]
  >

export type RedeemStakeInstructionData = { discriminator: ReadonlyUint8Array }

export type RedeemStakeInstructionDataArgs = {}

export function getRedeemStakeInstructionDataEncoder(): FixedSizeEncoder<RedeemStakeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REDEEM_STAKE_DISCRIMINATOR }),
  )
}

export function getRedeemStakeInstructionDataDecoder(): FixedSizeDecoder<RedeemStakeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ])
}

export function getRedeemStakeInstructionDataCodec(): FixedSizeCodec<
  RedeemStakeInstructionDataArgs,
  RedeemStakeInstructionData
> {
  return combineCodec(
    getRedeemStakeInstructionDataEncoder(),
    getRedeemStakeInstructionDataDecoder(),
  )
}

export type RedeemStakeInput<
  TAccountKeeper extends string = string,
  TAccountPerpetuals extends string = string,
  TAccountPool extends string = string,
  TAccountCustody extends string = string,
  TAccountStakeAccount extends string = string,
  TAccountStakeInfo extends string = string,
> = {
  keeper: TransactionSigner<TAccountKeeper>
  perpetuals: Address<TAccountPerpetuals>
  pool: Address<TAccountPool>
  custody: Address<TAccountCustody>
  stakeAccount: Address<TAccountStakeAccount>
  stakeInfo: Address<TAccountStakeInfo>
}

export function getRedeemStakeInstruction<
  TAccountKeeper extends string,
  TAccountPerpetuals extends string,
  TAccountPool extends string,
  TAccountCustody extends string,
  TAccountStakeAccount extends string,
  TAccountStakeInfo extends string,
  TProgramAddress extends Address = typeof PERPETUALS_PROGRAM_ADDRESS,
>(
  input: RedeemStakeInput<
    TAccountKeeper,
    TAccountPerpetuals,
    TAccountPool,
    TAccountCustody,
    TAccountStakeAccount,
    TAccountStakeInfo
  >,
  config?: { programAddress?: TProgramAddress },
): RedeemStakeInstruction<
  TProgramAddress,
  TAccountKeeper,
  TAccountPerpetuals,
  TAccountPool,
  TAccountCustody,
  TAccountStakeAccount,
  TAccountStakeInfo
> {
  // Program address.
  const programAddress = config?.programAddress ?? PERPETUALS_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    keeper: { value: input.keeper ?? null, isWritable: false },
    perpetuals: { value: input.perpetuals ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    custody: { value: input.custody ?? null, isWritable: true },
    stakeAccount: { value: input.stakeAccount ?? null, isWritable: false },
    stakeInfo: { value: input.stakeInfo ?? null, isWritable: true },
  }
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('keeper', accounts.keeper),
      getAccountMeta('perpetuals', accounts.perpetuals),
      getAccountMeta('pool', accounts.pool),
      getAccountMeta('custody', accounts.custody),
      getAccountMeta('stakeAccount', accounts.stakeAccount),
      getAccountMeta('stakeInfo', accounts.stakeInfo),
    ],
    data: getRedeemStakeInstructionDataEncoder().encode({}),
    programAddress,
  } as RedeemStakeInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountPerpetuals,
    TAccountPool,
    TAccountCustody,
    TAccountStakeAccount,
    TAccountStakeInfo
  >)
}

export type ParsedRedeemStakeInstruction<
  TProgram extends string = typeof PERPETUALS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    keeper: TAccountMetas[0]
    perpetuals: TAccountMetas[1]
    pool: TAccountMetas[2]
    custody: TAccountMetas[3]
    stakeAccount: TAccountMetas[4]
    stakeInfo: TAccountMetas[5]
  }
  data: RedeemStakeInstructionData
}

export function parseRedeemStakeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRedeemStakeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    )
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      keeper: getNextAccount(),
      perpetuals: getNextAccount(),
      pool: getNextAccount(),
      custody: getNextAccount(),
      stakeAccount: getNextAccount(),
      stakeInfo: getNextAccount(),
    },
    data: getRedeemStakeInstructionDataDecoder().decode(instruction.data),
  }
}
